export async function createUserSessionHandler( req: Request, res: Response ) {

    // Validate the email and password they should be matching basically
    
    // Create a session

    // create access token

    // create access token

    // 
}